# Every branch appends its own version rows to experiments.csv: keep the lines of both
# sides on merge, then re-sort and check for repeated versions (docs/plan/14 §8).
experiments/experiments.csv merge=union
