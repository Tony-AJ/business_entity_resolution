# Challenge data and generated submissions stay local (regenerable, large)
dataset/
output/
# one folder per uploaded version: the exact files sent to the portal
submissions/
*.zip

# Trained models and per-experiment artifacts (models, candidates, predictions)
models/
*.joblib
*.pkl
experiments/*/artifacts/

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Tool caches
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Personal AI assistant rules and settings: each teammate keeps their own
CLAUDE.md
CLAUDE.local.md
.claude/settings.local.json

# Secrets
.env
